# FileTopo 0.1.0 — superseded local candidate

This unpublished candidate was superseded by
[`0.1.0-alpha.1`](0.1.0-alpha.1.md). Its hashes are retained only as historical
verification evidence. None of the artifacts listed below may be distributed.

**Status:** unsigned, unpublished local candidate.
**Prepared:** 2026-08-26.
**Rebuilt with path remapping:** 2026-08-26, during `TASK-0008`.

## Highlights

- Several independent local collections with a name, colour and icon.
- Native folder selection and explicitly triggered indexing.
- Metadata-only scanning, cancellable, with no partial index.
- PixiJS/WebGL topographic map, relief fallback, pagination and level of detail.
- Search, filtering by kind, online-only files and unseen items.
- An accessible synchronised list and a French/English interface that follows
  the system language, with English as the fallback.
- Explicit, confined reveal in Windows File Explorer.
- A local SQLite index, rebuildable and stored outside the collections.

## Local verification

The candidate passed 36 interface tests, 13 Rust tests, TypeScript, Vite,
strict Clippy in both the debug and release profiles, the dependency audit and
the Windows/NSIS build. Synthetic measurements at 100 000 items are recorded in
`docs/performance/phase-4-mvp-measurements.md`.

## Artifacts

Built by `scripts/build-release-clean.ps1`, which remaps build-machine paths
out of the binary, then verified by
`scripts/scan-binary-for-personal-paths.ps1`, which found **no occurrence** of
the account name, user profile, repository root or Cargo home, in ASCII or
UTF-16LE.

SHA-256 of the unsigned local artifacts rebuilt on 2026-08-26:

- `filetopo.exe` (11 195 904 bytes):
  `71BEA4EFC76AAB8C66FBCF315BD903981450851FAED67972E45AE5BD712CB7B6`;
- `FileTopo_0.1.0_x64-setup.exe` (2 927 778 bytes):
  `BF71FF7EA6CA4DF178CF1F459E4891A422D047B5BA23A7E2D0826CF748124A72`.

These hashes replace the ones recorded during phase 5. Those earlier artifacts
embedded build-machine paths and **must not be distributed**.

## Known limits

- Windows 10/11 only; macOS and Linux are not validated.
- Full index rebuilds; incremental watching is not delivered.
- No application-level index encryption and no built-in erasure of application
  data.
- No network, account, synchronisation, AI, OCR or automatic updates.
- The local installer is unsigned; Windows may show a warning.
- The synthetic fixture button exists in development builds only.
- FileTopo remains a working name with no exhaustive legal clearance.

## Publication

These notes are not a release. No binary may be distributed before eventual
signing, a final check and the special human go-ahead for phase 6. The
`TASK-0008` review recommends publishing **source only**, with no binary.
